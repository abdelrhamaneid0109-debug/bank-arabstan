const merchantRoleId = "PUT_ROLE_ID_HERE";

if (!interaction.member.roles.cache.has(merchantRoleId)) {
  return interaction.reply({
    content: "❌ الأمر ده للتجار فقط",
    ephemeral: true
  });
}
const merchantRoleId = "PUT_ROLE_ID_HERE";

if (!interaction.member.roles.cache.has(merchantRoleId)) {
  return interaction.reply({
    content: "❌ الأمر ده للتجار فقط",
    ephemeral: true
  });
}
